# Anthropic Claude API key (required)
ANTHROPIC_API_KEY=sk-ant-...

# OpenAI API key for Whisper transcription (required for /api/demo/transcribe)
OPENAI_API_KEY=sk-...

# PostgreSQL connection string (required)
DATABASE_URL=postgresql://user:password@localhost:5432/narc

# JWT secret — must be a 32+ character random string in production
# Leave empty / unset for dev-only mock admin bypass (no token required)
JWT_SECRET=narc-dev-secret-change-in-production
# NARC_AUTH=true   # Uncomment to enforce JWT auth

# File upload directory for document attachments
UPLOAD_DIR=./uploads

# Server port (default: 3001)
PORT=3001

# CORS origins (comma-separated)
CORS_ORIGINS=http://localhost:5173,https://localhost:3000
